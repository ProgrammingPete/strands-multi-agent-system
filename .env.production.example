# Production Environment Configuration
# IMPORTANT: This file shows the required configuration for production deployments.
# Copy this to .env.production and fill in your values.

# AWS Configuration
AWS_REGION=us-east-1
# Note: In production, use IAM roles instead of AWS_PROFILE

# Bedrock Configuration
BEDROCK_MODEL_ID=global.amazon.nova-2-lite-v1:0

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
# IMPORTANT: Only use SUPABASE_PUB_KEY in production
# DO NOT include SUPABASE_SERVICE_KEY - it bypasses RLS security
SUPABASE_PUB_KEY=your-pub-key-here

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Environment Configuration
# MUST be set to 'production' for production deployments
ENVIRONMENT=production

# CORS Configuration (update with your production domains)
# CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com

# Note: SYSTEM_USER_ID should NOT be set in production
# Note: SUPABASE_SERVICE_KEY should NOT be set in production
